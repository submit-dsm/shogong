import PageContainer from "@/components/result/PageContaner";

interface Props {
  title: string;
}

const DetailResultPage = ({ title }: Props) => {
  return (
    <PageContainer title={title}>
      <></>
    </PageContainer>
  );
};

export default DetailResultPage;
